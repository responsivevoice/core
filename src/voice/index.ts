/**
 * Voice Resolution Module
 *
 * Provides the voice fallback resolution system for ResponsiveVoice.
 */

// Resolver
export { VoiceResolver } from './resolver';
// Types
export type { ResolvedVoice, VoiceSelector } from './types';
// Voice detection utilities
export { isGoogleRemoteVoice } from './types';
