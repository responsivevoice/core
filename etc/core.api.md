## API Report File for "@responsivevoice/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { FeatureManager } from '@responsivevoice/features';
import { ResponsiveVoiceAPIClient } from '@responsivevoice/api-client';
import { RVEventType } from '@responsivevoice/types';
import { SpeakParams } from '@responsivevoice/types';
import { SystemVoice } from '@responsivevoice/types';
import { TextChunk } from '@responsivevoice/text';
import { TransportMode } from '@responsivevoice/types';
import { TTSService } from '@responsivevoice/types';
import { Voice } from '@responsivevoice/types';
import { VoiceQuery } from '@responsivevoice/types';
import { VoiceSelector } from '@responsivevoice/types';
import { VoiceSelectorInput } from '@responsivevoice/types';
import { WebPlayerFeature } from '@responsivevoice/features';
import { WebsiteFeatures } from '@responsivevoice/types';

// @public
export type CacheScope = 'all' | 'voices' | 'server' | 'config';

// @public
export interface DebugTools {
    clearCache(scope?: CacheScope): Promise<void>;
}

// @public
export function detectPlatform(): PlatformInfo;

// @public
export function extractPlatformVersionInfo(platformInfo: PlatformInfo): PlatformVersionInfo;

// @public
function getResponsiveVoice(options?: ResponsiveVoiceInitOptions): Promise<ResponsiveVoice>;
export default getResponsiveVoice;
export { getResponsiveVoice }

// @public
export interface PermissionConfig {
    allowPermissionPopupEverywhere?: boolean;
    disablePermissionPopup?: boolean;
}

// @public
export interface PermissionPopupOptions {
    allowLabel?: string;
    appendTo?: HTMLElement;
    classPrefix?: string;
    denyLabel?: string;
    position?: 'top' | 'bottom';
    renderPopup?: (onAllow: () => void, onDeny: () => void) => HTMLElement;
    textOverride?: string;
    urlOverride?: string;
}

// @public
export interface PlatformInfo {
    hasIOSAudioUnlockBug: boolean;
    iOSVersion: number;
    isAndroid: boolean;
    isChrome: boolean;
    isEdge: boolean;
    isFirefox: boolean;
    isIOS: boolean;
    isIOS10: boolean;
    isIOS11Plus: boolean;
    isIOS12: boolean;
    isIOS9: boolean;
    isLinux: boolean;
    isMacOS: boolean;
    isOpera: boolean;
    isSafari: boolean;
    isWindows: boolean;
    requiresUserInteraction: boolean;
    supportsAudioElement: boolean;
    supportsSendBeacon: boolean;
    supportsWebSpeech: boolean;
    useTimerForEvents: boolean;
}

// @public
export interface PlatformVersionInfo {
    browser: string;
    browserVersion: string;
    deviceType: 'desktop' | 'mobile' | 'tablet';
    os: string;
    osVersion: string;
}

// @public
export function resetResponsiveVoice(): void;

// @public
export type ResolveVoiceHook = (selector: VoiceSelector | undefined) => VoiceSelector | undefined;

// @public
export class ResponsiveVoice extends ResponsiveVoiceCore {
    addEventListener(event: RVEventType, callback: GenericEventCallback): void;
    get allowPermissionPopupEverywhere(): boolean;
    set allowPermissionPopupEverywhere(value: boolean);
    allowSpeechClicked(allowed: boolean): void;
    applyTextReplacements(text: string): string;
    chunkText(text: string, options?: {
        characterLimit?: number;
    }): TextChunk[];
    clearFallbackPool(): void;
    clickEvent(): void;
    get clickEventDetected(): boolean;
    get debug(): boolean;
    set debug(enabled: boolean);
    get debugTools(): DebugTools | undefined;
    get disablePermissionPopup(): boolean;
    set disablePermissionPopup(value: boolean);
    get enableEstimationTimeout(): boolean;
    set enableEstimationTimeout(value: boolean);
    enableWindowClickHook(): void;
    get fallbackMode(): boolean;
    getAvailableVoices(): string[];
    getBrowserVoiceCount(): number;
    getBrowserVoices(): SpeechSynthesisVoice[];
    getCharacterLimit(): number;
    getConfig(): {
        apiKey: string;
        defaultVoice: string;
        defaultParams: SpeakParams;
    };
    getDefaultVoice(): string;
    getEstimatedTimeLength(text: string, multiplier?: number): number;
    getOutputDevice(): string | null;
    getPlatformInfo(): PlatformInfo;
    getPlatformVersionInfo(): PlatformVersionInfo;
    getServiceEnabled(service: number): boolean;
    getServicePriority(): number[];
    getVoices(): {
        name: string;
        flag: string;
        gender: "f" | "m";
        lang: string;
        voiceIDs: number[];
        deprecated?: boolean | undefined;
        isByok?: boolean | undefined;
        provider?: string | undefined;
    }[];
    getVolume(): number;
    hidePermissionPopup(): void;
    isDemoMode(): boolean;
    isFallbackAudioPlaying(): boolean;
    isForceFallback(): boolean;
    isNativeAvailable(): Promise<boolean>;
    isNativeSupported(): boolean;
    isReady(): boolean;
    log(message: string): void;
    get outputDevice(): string | null;
    set outputDevice(deviceId: string | null);
    get prosodyFallback(): boolean;
    set prosodyFallback(value: boolean);
    removeEventListener(event: RVEventType, callback: GenericEventCallback): void;
    setCharacterLimit(limit: number): void;
    setDefaultRate(rate: number): void;
    setDefaultVoice(voice: string): void;
    setForceFallback(force: boolean): void;
    setOutputDevice(deviceId: string): Promise<void>;
    setServiceEnabled(service: number, enabled: boolean): void;
    setServicePriority(priority: number[]): void;
    setTextReplacements(rules: TextReplacementRule[] | null): void;
    setVolume(volume: number): void;
    showPermissionPopup(options?: PermissionPopupOptions): void;
    get speechAllowedByUser(): boolean | null;
    readonly version: string;
    voiceSupport(): boolean;
}

// @public
export interface ResponsiveVoiceInitOptions {
    apiBaseUrl?: string;
    apiKey?: string;
    apiSecret?: string;
    autoConnect?: boolean;
    characterLimit?: number;
    defaultParams?: Partial<SpeakParams>;
    defaultVoice?: string;
    enableAnalytics?: boolean;
    enableDOMEvents?: boolean;
    enableVoiceReporting?: boolean;
    features?: Partial<WebsiteFeatures>;
    forceFallback?: boolean;
    permissionConfig?: PermissionConfig;
    prosodyFallback?: boolean;
    resolveVoice?: ResolveVoiceHook;
    transport?: TransportMode;
}

// @public
export interface TextReplacementRule {
    collectionvoices?: string | string[];
    newvalue: string;
    searchvalue: string | RegExp;
    systemvoices?: string | string[];
}

export { VoiceSelector }

```
